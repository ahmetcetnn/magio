{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "npm prune and remove unused deps",
			"type": "shell",
			"command": "powershell",
			"args": [
				"-NoProfile",
				"-Command",
				"npm prune; npm ls --json | Out-String"
			]
		}
	]
}